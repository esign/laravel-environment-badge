{
    "name": ":vendor/:package_slug",
    "description": ":package_description",
    "keywords": [
        ":vendor",
        ":package_name"
    ],
    "homepage": "https://github.com/:vendor/:package_slug",
    "license": "MIT",
    "type": "library",
    "authors": [
        {
            "name": ":author_name",
            "email": ":author_email",
            "role": "Developer"
        }
    ],
    "require": {
        "php": "^8.0",
        "illuminate/support": "^8.0"
    },
    "require-dev": {
        "friendsofphp/php-cs-fixer": "^3.5",
        "orchestra/testbench": "^6.0",
        "phpunit/phpunit": "^9.0"
    },
    "autoload": {
        "psr-4": {
            ":vendor_namespace_escaped\\": "src"
        }
    },
    "autoload-dev": {
        "psr-4": {
            ":vendor_namespace_escaped\\Tests\\": "tests"
        }
    },
    "scripts": {
        "test": "vendor/bin/phpunit",
        "phpcs": "vendor/bin/php-cs-fixer fix"
    },
    "config": {
        "sort-packages": true
    },
    "extra": {
        "laravel": {
            "providers": [
                ":vendor_namespace_escaped\\:studly_package_nameServiceProvider"
            ],
            "aliases": {
                ":studly_package_name": ":vendor_namespace_escaped\\Facades\\:studly_package_nameFacade"
            }
        }
    },
    "minimum-stability": "dev",
    "prefer-stable": true
}
